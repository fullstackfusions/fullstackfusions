# Blog Authoring Reference

**Drafts:** `blog/_drafts/` — never picked up by the build script
**Build command:** `node scripts/build-blog.js`
**Preview:** `node scripts/build-blog.js && python3 -m http.server 8080`

Every run of the build command regenerates three things automatically — you never edit them by hand:

| File | What it is |
|---|---|
| `blog/index.html` | The blog listing page (shell only — posts are rendered dynamically from `feed.json` via JS, enabling search and tag filtering) |
| `blog/feed.json` | All posts feed — used by the blog index (search/filter) and the homepage widget |
| `blog/posts/<slug>/index.html` | Each individual post page (Markdown posts only) |

---

## 1. Markdown Posts

### File location

- Source file goes in `_posts/` with the filename format `YYYY-MM-DD-slug.md`
- The build script converts it to `blog/posts/YYYY-MM-DD-slug/index.html` automatically
- After adding or removing a post, run `node scripts/build-blog.js` to rebuild the listing

### Images

Drop images into the **generated output folder**, not next to the `.md` source:

```
blog/posts/
└── 2026-03-14-my-post/
    ├── index.html      ← auto-generated by build script, do not edit
    ├── photo.jpg       ← your image goes here
    └── diagram.png
```

Reference them in Markdown with a relative path:

```markdown
![Alt text](./photo.jpg)
```

### Frontmatter

Every `.md` file must start with a frontmatter block:

```markdown
---
title: "Your Post Title"
date: 2026-03-14
description: "One sentence summary shown in the listing and meta tag."
tags: ["engineering", "tools"]
---

Your post content in plain Markdown here.
```

---

## 2. HTML / JavaScript / CSS Posts

### File location

Create a folder directly inside `blog/posts/` and write your `index.html` there:

```
blog/posts/
└── 2026-05-01-my-interactive-post/
    ├── index.html      ← write this by hand
    ├── style.css       ← post-specific CSS (optional)
    ├── script.js       ← post-specific JS (optional)
    └── screenshot.png  ← images (optional)
```

### Referencing assets

All assets (CSS, JS, images) live in the **same folder** as `index.html`. Reference them with relative paths:

```html
<link rel="stylesheet" href="./style.css" />
<script src="./script.js"></script>
<img src="./screenshot.png" alt="..." />
```

### Shared blog styles

Global blog styles (`.blog-container`, `.post-header`, `.post-content`, `.tag`, etc.) live in `blog/assets/blog.css`. Load it the same way generated posts do — use an absolute path so it resolves correctly from any post folder:

```html
<link rel="stylesheet" href="/blog/assets/blog.css" />
```

Post-specific styles go into the post's own `style.css` — do **not** edit `blog/assets/blog.css` for per-post styles.

### blog-meta comment (required to appear in the listing)

The build script won't auto-discover custom HTML posts unless you add this comment near the top of `index.html`. The build script reads it and includes the post in `blog/index.html` and `feed.json`:

```html
<!--blog-meta
{
  "title": "My Interactive Post",
  "date": "2026-05-01",
  "description": "One sentence summary.",
  "tags": ["interactive", "data"]
}
-->
```

After adding the comment, rebuild:

```bash
node scripts/build-blog.js
```

### Starter template

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Post Title — fullstackfusions</title>
  <link rel="icon" sizes="32x32" type="image/png" href="/images/logo.png" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/blog/assets/blog.css" />
  <!-- Post-specific styles (optional) -->
  <link rel="stylesheet" href="./style.css" />
  <script>(function(){const s=localStorage.getItem('theme');if(s==='dark'||(!s&&window.matchMedia('(prefers-color-scheme: dark)').matches)){document.documentElement.classList.add('dark');}})();</script>
</head>
<body class="bg-gray-50 text-gray-900">
  <!--blog-meta
  {
    "title": "Post Title",
    "date": "2026-05-01",
    "description": "One sentence summary.",
    "tags": ["tag1", "tag2"]
  }
  -->

  <button id="theme-toggle" onclick="toggleTheme()" aria-label="Toggle dark mode" title="Toggle dark/light mode"><span id="theme-icon">&#x1F319;</span></button>

  <header class="bg-gray-900 text-white">
    <nav class="border-b border-gray-700">
      <div class="container mx-auto px-4 py-3">
        <ul class="flex flex-wrap justify-center items-center gap-x-6 gap-y-2 text-sm md:text-base">
          <li><a href="/" aria-label="Home" class="flex items-center"><img src="/images/logo.png" alt="Home" style="width:1.5rem;height:1.5rem;" /></a></li>
          <li><a href="/#about" class="hover:text-blue-400">About</a></li>
          <li><a href="/#skills" class="hover:text-blue-400">Skills</a></li>
          <li><a href="/projects" class="hover:text-blue-400">Projects</a></li>
          <li><a href="/experience" class="hover:text-blue-400">Experience</a></li>
          <li><a href="/blog" class="hover:text-blue-400">Blog</a></li>
        </ul>
      </div>
    </nav>
  </header>

  <main class="blog-container">
    <a class="back-link" href="/blog">&#8592; All posts</a>
    <article>
      <header class="post-header">
        <div class="post-meta">May 1, 2026</div>
        <h1 class="text-3xl font-bold mt-2">Post Title</h1>
        <p class="text-gray-600 mt-2">One sentence description.</p>
        <div class="post-tags"><span class="tag">tag1</span></div>
      </header>
      <div class="post-content">
        <!-- Your content here -->
      </div>
    </article>
  </main>

  <!-- Post-specific JS (optional) -->
  <script src="./script.js"></script>
  <script>
    function toggleTheme(){const d=document.documentElement.classList.toggle('dark');localStorage.setItem('theme',d?'dark':'light');document.getElementById('theme-icon').textContent=d?'\u2600\uFE0F':'\uD83C\uDF19';}
    document.getElementById('theme-icon').textContent=document.documentElement.classList.contains('dark')?'\u2600\uFE0F':'\uD83C\uDF19';
  </script>
</body>
</html>
```
